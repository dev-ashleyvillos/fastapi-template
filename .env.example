# Specify absolute path of your python binary if using a wsgi server that needs to use the passenger_wsgi.py
PYTHON_DIR=

# Set DEBUG=true, if you need to see traceback of the errors in the API
# Set DEBUG=false, if you only need the basic error messages
DEBUG=

# Set RELOAD=true for development (hot reload on code changes)
# Set RELOAD=false for production (better performance and stability)
RELOAD=

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=